# Cheddar Battles CSS and design tokens

For the outcome-spec asset bundle (§18). game.css is extracted from the shipped single-file game; globals.css is the Next.js app's token sheet; design-system.md is the written system. Cheese-theme palette values are theme-bound (R12); structure and roles are fixed (R11).
